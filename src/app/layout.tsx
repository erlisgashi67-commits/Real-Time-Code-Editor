import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Toaster } from "@/components/ui/sonner";
import { ThemeProvider } from "@/components/theme-provider";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "CodeSync — Real-time Collaborative Code Editor",
  description:
    "A mini GitHub Codespaces + Google Docs for code. Multi-user live editing, file tree, in-browser execution, version history, comments, chat, and shareable links.",
  keywords: [
    "CodeSync",
    "collaborative editor",
    "real-time code",
    "Monaco",
    "Next.js",
    "pair programming",
  ],
  authors: [{ name: "CodeSync" }],
  openGraph: {
    title: "CodeSync",
    description: "Real-time collaborative code editor",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-background text-foreground`}
      >
        <ThemeProvider
          attribute="class"
          defaultTheme="dark"
          enableSystem
          disableTransitionOnChange
        >
          {children}
          <Toaster richColors position="bottom-right" />
        </ThemeProvider>
      </body>
    </html>
  );
}
